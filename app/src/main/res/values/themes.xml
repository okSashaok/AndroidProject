<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NMedia" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NMedia" parent="Base.Theme.NMedia" />

    <style name="Theme.NMedia.Button.TextButton.Icon.StatisticsButton" parent="Widget.Material3.Button.TextButton.Icon">
        <item name="android:checkable">true</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:textColor">#888</item>
        <item name="iconTint">@color/statistics_button_icon</item>
        <item name="backgroundTint">@color/statistics_button_background</item>
    </style>
</resources>